{
  "_comentario": "Configuración de la API. La cadena de conexión por defecto apunta a localhost:15454 (el puerto PUBLICADO de PostgreSQL) para correr la API sin Docker. Dentro de Docker, el compose la sobreescribe con la variable ConnectionStrings__Postgres (host interno postgres:5432).",
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=15454;Database=bdfacturas_postgres_local;Username=postgres;Password=Diseno123!"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
